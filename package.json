{
  "private": true,
  "scripts": {
    "install:backend": "python -m pip install -r backend/requirements.txt || python3 -m pip install -r backend/requirements.txt",
    "build": "npm run install:backend && npm --prefix frontend ci && npm --prefix frontend run build",
    "start": "node railway-start.js"
  }
}
