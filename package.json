{
  "private": true,
  "scripts": {
    "install:backend": "python -m venv .venv && .venv\\Scripts\\python.exe -m pip install -r backend/requirements.txt",
    "build": "npm --prefix frontend run build",
    "start": "node railway-start.js"
  }
}
